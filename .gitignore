# Рабочие markdown-доки в корне — не публиковать (брифы, handoff, заметки).
# Слэш в начале = только корень; markdown внутри docs/ и builder/ не трогаем.
/*.md

# Сырьё, превью-экспорты и копии — не публиковать.
_source_raw/
*.html.bak
